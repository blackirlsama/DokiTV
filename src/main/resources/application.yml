spring:
  application:
    name: tianmu
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.76.96.24:3306/tianmu?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: gK3T9n%q2M@j7Z9

  data:
    redis:
      database: 15
      host: 47.115.130.44
      port: 59000
      timeout: 50000
      password: " "
      #    password: e65K4t8w2 # Redis 服务器密码（当前未启用）
      # 配置 redis 连接池
      lettuce:
        pool:
          # 链接池中最小的空闲链接 默认为 0
          min-idle: 4
          # 链接池中最大的空闲连接 默认为 8
          max-idle: 8
          #连接池中最大数据库链接数 默认为 8
          max-active: 8
          #连接池最大阻塞等待时间
          max-wait: 5000ms

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名
      logic-delete-value: 1 # 逻辑已删除值（默认为 1）
      logic-not-delete-value: 0 # 逻辑未删除值（默认为 0）

minio:
  url: http://120.76.96.24:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: tianmu-upload


netty:
  port: 9100


rocketmq:
  name-server: 127.0.0.1:9876
  producer:
    group: zzz-producer-group
  consumer:
    group: zzz-consumer-group
server:
  port: 8101